# Notes

- Background: comfortable with general chemistry/materials concepts, but no prior composites-specific knowledge. Lessons can assume basic polymer/chemistry vocabulary but must define composites-specific terms from scratch.
- Wants breadth first: all four segments (boats, PWC, kayak/canoe, pools) in view from the start, rather than picking one segment to go deep on immediately.
- Still deciding which resin/brand to represent. Until that's settled, keep lessons resin-family-agnostic (polyester/vinyl ester/epoxy landscape) rather than pitching one product.
- No stated preference yet on joining a community (e.g. Boat Design Net forum) — ask before assuming.
- Chose to go deep on the boats segment first (of the four in MISSION.md). Lessons 0002–0003 cover it: manufacturing processes, then the two real purchasing drivers (styrene/VOC regulation, blister warranty economics). PWC/kayak/pool segments still untouched.
- Asked specifically for Canadian boat-building resin specifications (Lesson 0004) — suggests the Canadian market matters for this BD effort, not just US. Worth asking directly whether the primary target market is Canada, US, or both, next time it's relevant (e.g. before choosing which segment/region to go deep on next).
- Flagged in RESOURCES.md: `tc.canada.ca` and `laws-lois.justice.gc.ca` are not fetchable from this session's network — Lesson 0004's regulatory claims are sourced from search summaries only, not a direct primary-source read. Treat as needing verification, and say so if the user wants to act on it commercially.
- Requested a deeper dive on blistering specifically, plus wanted a specific quote (Potts, via Soundings, on the no-sanding-primer catch on vinylester-skin-coat blister warranties) incorporated. Both now live in Lesson 0005. `westsystem.com` and `soundingsonline.com` turned out to be unreachable too (same network pattern as tc.canada.ca) — worth telling the user proactively when this comes up again rather than re-discovering it each time.
- Wants a universal collapsible jump-menu on every page, not per-lesson prev/next links only. Added as `assets/nav.css` + `assets/nav.js`, wired into every existing page. **Every future lesson/reference page must include both** (`<link rel="stylesheet" href="../assets/nav.css">` after the style.css link, `<script src="../assets/nav.js" defer></script>` before `</body>`) **and** get a new entry added to `WORKSPACE_PAGES` in `assets/nav.js` — the list isn't auto-discovered, it's hand-maintained.
- Asked for more detail on the specific Transport Canada ABYC-alternative policy URL. Confirmed WebFetch is entirely blocked in this session (not just tc.canada.ca — see learning record 0002) — pushed harder on WebSearch instead and found real added detail (two-tier scope, Scholten quote, 3rd-party corroboration) plus a real correction (my earlier "ABYC laminate/construction standards" phrasing overstated it — ABYC's standards here look to be systems/equipment, not structural/resin specs). User seems comfortable with iterative fact-finding via search when direct fetch isn't possible — keep pushing WebSearch rather than giving up after one blocked attempt.
- User then supplied a direct TP1332E PDF URL (path dated 2023-11). Still couldn't fetch it (same blanket WebFetch block), but searching around it surfaced two things worth remembering: (1) TP 1332 has editions through 2024 — this workspace had been citing the 2010 edition throughout, now corrected; (2) the flame-retardant-resin-in-engine-space claim resurfaced verbatim via an independent search path, upgrading it from unverified to corroborated. User is willing to try supplying source URLs directly when fetch fails — worth suggesting they upload the actual PDF file next, since that would let it be read directly via the pdf skill/Read tool rather than pieced together from search snippets.
