# Notes

- Workspace created 2026-08-04, mirroring the structure of the `trenchless-technologies` workspace elsewhere in this repo.
- Mission established via direct question: BD/sales at Kanata Electronics, chasing subcontract opportunities off Canadian defence primes.
- Kanata Electronics is a Tier 2/3 electronics/cable-assembly supplier (inferred from surrounding session context, not yet confirmed directly with the user) — lessons should assume that supply-chain position unless corrected.
- Kanata already runs a real BD tracking tool: `lx4ever/kanata-news-feed` (GitHub), which exports `Kanata_Canadian_Programs_Obligations_*.xlsx` — a curated list of Canadian programs, a live opportunity pipeline (Notion-sourced), and the full ISED ITB obligations register. Reviewed 2026-08-04. When a lesson covers a program that's in that workbook, cross-check figures against it and cite it as a Kanata-specific source (not just a generic public one) — it already has named ITB contacts and scored pipeline entries per program.
- **Confirmed, not inferred** (found in `kanata-crm-excel-sync.skill`'s own SKILL.md, extracted 2026-08-05): "Mimi runs a Notion 'CRM — Business Development Hub' for Kanata Electronics (an electrical-assembly / wire-harness SMB pursuing Canadian defence & space work)." The user's name is Mimi. This is the authoritative capability description — use it verbatim for any "does X program actually fit Kanata" judgment, rather than inferring from program language alone.
- The workbook's curated 21-program list is NOT auto-generated from a capability match — it's a manually maintained editorial whitelist in `assets/config.json` (inside the `.skill` bundle at `skills/kanata-crm-excel-sync.skill` in the kanata-news-feed repo) filtering Notion's Program Map, which is itself polluted by RSS auto-ingest. A program's absence (e.g. the whole Land domain, Lesson 9) means nobody has added it to that whitelist yet — not that it was evaluated and rejected for fit.
